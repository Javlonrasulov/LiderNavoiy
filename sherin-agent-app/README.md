# Sherin Agent (mobil)

Agent ilovasi — alohida loyiha. Admin panel `../sherin-admin-app` da.

## Ishga tushirish

```bash
npm install --legacy-peer-deps
npm run dev
```

## Sahifalar

- `/` — Asosiy (home)
- `/products`, `/clients`, `/visit`, `/order-summary`
- `/plan`, `/messages`, `/profile`
- `/liniya`, `/sotrudniki`, `/locatsiya`
