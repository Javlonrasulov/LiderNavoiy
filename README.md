
  # Sherin CRM

  This is a code bundle for Sherin CRM. The original project is available at https://www.figma.com/design/jm6C7zdOedSzHrvSIyarMV/Sherin-CRM.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  